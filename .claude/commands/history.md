Show all Tcl commands that were sent to the EDA terminal in this session.

Review the session history by checking /tmp/hipilot_exec_*.tcl and /tmp/hipilot_generated_*.tcl files.

Steps:
1. List all hipilot temp files from this session
2. Show each command with timestamp
3. Indicate which were template-based vs AI-generated
4. Show execution status if available
